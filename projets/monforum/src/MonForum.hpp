#pragma once

#include <map>
#include <string>
#include <vector>

using IdMessage = std::string;
using IdDiscussion = std::string;

struct Message {
  std::string _auteur;
  std::string _contenu;
};

