#pragma once

#include "Paint.hpp"

#include <string>

// Paint
const double gDefaultTickness = 10;
const Color gDefaultColor {0, 0, 0};

// Ui
const std::string gTitle = "paint";
const int gWindowWidth = 800;
const int gWindowHeight = 600;
const int gPanelWidth = 150;
const int gMaxTickness = 100;
const Color gBgColor {1, 1, 1};

